---
description: Run safe git status check, worktree management, or author atomic semantic commits.
---

Execute the `git-master` workflow for: "$*"

1. Inspect `git status` and diffs safely without destructive commands.
2. If committing, author atomic conventional commits (`feat`, `fix`, `refactor`) with clear changelog bullet points.
3. If managing task isolation, provision worktrees under `~/.omp/wt/`.
